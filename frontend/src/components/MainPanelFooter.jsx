const MainPanelFooter = ({ id, className }) => {
    return (
        <footer id={id} className={`${className} mt-auto`}>
            © 2024 Rebecca.ai. All rights reserved.
        </footer>
    );
};

export default MainPanelFooter;